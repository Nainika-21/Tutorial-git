pipeline { 
  agent any 
  stages { 
    stage('Build') { 
      steps { echo 'Build project' } 
    } 
    stage('Test') {
      steps { echo 'Run the tests' } 
    } 
    stage('Deploy') { 
      steps { echo 'Deploy application' } 
    } 
  } 
}